"""Procedural authoring library for the chapters in ../scenes/.

Runs inside Blender only (imports `bpy`). Entry point for a scene script is
`build.finalize()`, which handles everything after the geometry is described.
"""
